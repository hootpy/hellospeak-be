Access Docker API
```shell
docker exec -it hellospeak-api /bin/sh
```

To make migrations
```shell
python manage.py makemigrations
python manage.py migrate
```

To run migrations
```shell
python manage.py migrate
```
